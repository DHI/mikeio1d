; this is the pytest configuration file used by the CI pipeline
[pytest]
testpaths = tests
addopts = 
    --strict-markers
    --durations=50
    --durations-min=0.5
    --disable-warnings
markers =
    slow
    optional_dependency
